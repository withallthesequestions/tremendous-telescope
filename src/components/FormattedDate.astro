---
const { date } = Astro.props;
---

<time>
	{
		date
			.toLocaleDateString('en-US', {
				timeZone: 'UTC',
				year: '2-digit',
				month: '2-digit',
				day: '2-digit',
			})
			.replaceAll('/', '')
	}
</time>
